// Same generated card as the OpenGraph image, exposed at the twitter-image route so
// summary_large_image cards resolve their own asset.
export { default, alt, size, contentType } from './opengraph-image';
