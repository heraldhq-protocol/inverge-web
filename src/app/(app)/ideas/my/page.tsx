import { MyIdeasView } from '@/components/ideas/my-ideas-view';

export const metadata = {
  title: 'My Ideas & Campaigns — Inverge',
  description: 'Manage your creator pitches, validation status, and campaign milestones.',
};

export default function MyIdeasPage() {
  return <MyIdeasView />;
}
